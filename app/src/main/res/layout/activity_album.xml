<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/white"
        android:orientation="horizontal">

        <TextView
            style="@style/activity_title_text_style"
            android:text="@string/album_activity_header_title" />

        <Button
            android:id="@+id/album_activity_shuffle_button"
            style="@style/shuffle_button_style"
            android:text="@string/header_shuffle_button_text" />
    </LinearLayout>

    <TextView
        style="@style/purpose_text_style"
        android:text="@string/album_activity_purpose_text" />

    <GridView
        android:id="@+id/grid_view_album"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:drawSelectorOnTop="true"
        android:gravity="center"
        android:numColumns="auto_fit"
        android:stretchMode="columnWidth" />
</LinearLayout>